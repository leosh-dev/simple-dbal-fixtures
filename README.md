# It`s simple example of fixtures on DBAL connection.

1) Create own fixture class and implement FixtureInterface.
2) Use `FixtureManager::loadFixtures` method to apply fixtures.
3) Maybe it will be useful for you https://github.com/dmaicher/doctrine-test-bundle
